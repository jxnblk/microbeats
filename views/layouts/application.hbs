<!DOCTYPE html>
<html
  ng-app="app"
  ng-controller="MainCtrl">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/base.min.css">
</head>
<body>

  <header class="sm-sticky top-0 px2 bg-white">
    <div class="center container py2 border-bottom">
      <h1 class="h2 bold caps m0">
        <span ng-hide="player.currentTrack">
          {{ title }}
        </span>
        <span class="ng-hide" ng-show="player.currentTrack">
          {% player.currentTrack.title %}
        </span>
      </h1>
      <div class="flex flex-center">
        <button class="flex-grow button-transparent" ng-click="player.previous()">
          {{icon "previous" }}
        </button>
        <button class="flex-grow h2 button-transparent bg-lighter-gray" ng-hide="player.playing" ng-click="player.play(player.i)">
          {{icon "play" }}
        </button>
        <button class="flex-grow h2 button-transparent" class="init-hide" ng-show="player.playing" ng-click="player.pause()">
          {{icon "pause" }}
        </button>
        <button class="flex-grow button-transparent bg-lighter-gray" ng-click="player.next()">
          {{icon "next" }}
        </button>
      </div>
      <div>
        <progress class="progress progress-blue mt1" value="{% currentTime / duration %}" ng-click="player.seek($event)"></progress>
        <div class="h6 flex">
          <div class="ng-hide" ng-show="currentTime >= 0">{% currentTime | time %}</div>
          <div class="flex-grow">&nbsp;</div>
          <div class="right-align">{% duration | time %}</div>
        </div>
      </div>
    </div>
  </header>

  {{#block "body" }}
    empty body
  {{/block}}

  <footer class="h5 container p2">
    <p>
      Microbeats is an experiment in music production emphasizing quantity over quality. All loops are created by Jxnblk in under an hour and not mixed down or mastered.
    </p>
    <div class="center mxn2 sm-flex">
      <a href="http://jxnblk.com" class="flex-grow button">Created by Jxnblk</a>
      <a href="http://creativecommons.org/licenses/by-nc-sa/3.0/" class="flex-grow button">Licensed under Creative Commons</a>
      <a href="http://soundcloud.com/jxnblk" class="flex-grow button">SoundCloud</a>
    </div>
  </footer>

  <script src="//ajax.googleapis.com/ajax/libs/angularjs/1.3.4/angular.min.js"></script>
  <script src="/js/app.min.js"></script>

</body>
</html>
