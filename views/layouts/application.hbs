<!DOCTYPE html>
<html
  ng-app="app"
  ng-controller="MainCtrl">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/base.min.css">
</head>
<body>

  <header class="border-bottom">
    <div class="container p2">
      <h1 class="bold m0">
        <span ng-hide="player.currentTrack">
          {{ title }}
        </span>
        <span ng-show="player.currentTrack">
          {% player.currentTrack.title %}
        </span>
      </h1>
      <button ng-click="player.previous()">
        {{icon 'previous' }}
      </button>
      <button ng-hide="player.playing" ng-click="player.play(player.i)">
        {{icon 'play' }}
      </button>
      <button ng-show="player.playing" ng-click="player.pause()">
        {{icon 'pause' }}
      </button>
      <button ng-click="player.next()">
        {{icon 'next' }}
      </button>
      <div>
        <progress value="{% currentTime / duration %}"></progress>
        <p>{% currentTime %} / {% duration %} {% currentTime / duration %}</p>
      </div>
    </div>
  </header>

  {{#block "body" }}
    empty body
  {{/block}}

  <footer>
    <p>
      Microbeats is an experiment in music production emphasizing quantity over quality. All loops are created by Jxnblk in under an hour and not mixed down or mastered.
    </p>
    <p>
      <a href="http://jxnblk.com">Created by Jxnblk</a>
      <a href="http://creativecommons.org/licenses/by-nc-sa/3.0/">Licensed under Creative Commons</a>
      <a href="http://soundcloud.com/jxnblk">SoundCloud</a>
    </p>
  </footer>

  <script src="//ajax.googleapis.com/ajax/libs/angularjs/1.3.4/angular.min.js"></script>
  <script src="/js/app.min.js"></script>

</body>
</html>
