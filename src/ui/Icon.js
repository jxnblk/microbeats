import styled from 'styled-components'
import { space, fontSize, width, color } from 'styled-system'

const Icon = styled('div')([], props => ({}), space, fontSize, width, color)

Icon.defaultProps = {}

export default Icon
